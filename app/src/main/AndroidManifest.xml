<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <uses-feature
        android:name="android.hardware.telephony"
        android:required="false" />

    <application
        android:name=".GycssApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Gycss2">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Gycss2.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.auth.RoleSelectionActivity" />
        <activity android:name=".ui.auth.LoginActivity" />
        <activity android:name=".ui.auth.RegisterActivity" />

        <activity android:name=".ui.senior.SeniorDashboardActivity" />
        <activity android:name=".ui.senior.LanguageSelectionActivity" />
        <activity android:name=".ui.senior.MedicalRecordsActivity" />
        <activity android:name=".ui.senior.RequestAssistanceActivity" />
        <activity android:name=".ui.senior.MedicationRemindersActivity" />
        <activity android:name=".ui.senior.profile.ProfileActivity" />
        <activity android:name=".ui.senior.settings.SettingsActivity" />

        <activity android:name=".ui.volunteer.VolunteerDashboardActivity" />
        <activity android:name=".ui.volunteer.help.AvailableRequestsActivity" />
        <activity android:name=".ui.volunteer.help.VolunteerTasksActivity" />
        <activity android:name=".ui.volunteer.profile.VolunteerProfileActivity" />
        <activity android:name=".ui.chat.ChatActivity" />

        <activity android:name=".ui.common.MapActivity" />
        <activity android:name=".ui.common.VolunteersListActivity" />
        <activity android:name=".ui.common.VolunteerReviewActivity" />

        <!-- Alarm Receiver -->
        <receiver
            android:name=".service.ReminderReceiver"
            android:exported="false" />

        <!-- Boot Receiver -->
        <receiver
            android:name=".service.BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Firebase Messaging Service -->
        <service
            android:name=".service.MyFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

    </application>

</manifest>
